{
  "name": "crawler-node",
  "version": "0.1.0",
  "private": true,
  "description": "Warm-profile browser harvesting: persistent session reuse, same-origin API calls, credentialed media capture",
  "scripts": {
    "test": "node --test test/"
  },
  "dependencies": {
    "puppeteer-core": "^22.15.0"
  }
}
